<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recommendation.intelligentoutfitrecommendationsystem.inventory.mapper.InventoryMapper">

    <!--
        给 Python AI 服务返回 SKU 实时库存视图。
        in_stock 在 SQL 中计算，保证 Java 后端和 AI 服务看到同一套可购买判断。
    -->
    <select id="findBySkuId" resultType="com.recommendation.intelligentoutfitrecommendationsystem.inventory.model.InventoryView">
        SELECT
            sku.id AS sku_id,
            sku.sku_code,
            sku.spu_id,
            p.name AS product_name,
            co.name AS color,
            so.code AS size,
            inv.available_stock,
            inv.locked_stock,
            inv.sold_stock,
            CASE WHEN inv.available_stock &gt; 0 THEN TRUE ELSE FALSE END AS in_stock
        FROM inventory inv
        JOIN product_sku sku ON sku.id = inv.sku_id
        JOIN product_spu p ON p.id = sku.spu_id
        JOIN color co ON co.id = sku.color_id
        JOIN size_option so ON so.id = sku.size_id
        WHERE sku.id = #{skuId}
    </select>

    <!--
        订单创建阶段使用条件 UPDATE 原子锁库存，避免先查后改导致并发超卖。
        这里仅从 available_stock 转入 locked_stock，支付成功后才会转入 sold_stock。
    -->
    <update id="lockStock">
        UPDATE inventory
        SET available_stock = available_stock - #{quantity},
            locked_stock = locked_stock + #{quantity},
            updated_at = CURRENT_TIMESTAMP(6)
        WHERE sku_id = #{skuId}
          AND available_stock &gt;= #{quantity}
    </update>

    <!--
        支付成功阶段只确认订单已经锁定的库存，不能再读取实时价格或重新判断购物车。
        条件 UPDATE 保证重复支付或库存状态异常时不会把 locked_stock 扣成负数。
    -->
    <update id="confirmSoldStock">
        UPDATE inventory
        SET locked_stock = locked_stock - #{quantity},
            sold_stock = sold_stock + #{quantity},
            updated_at = CURRENT_TIMESTAMP(6)
        WHERE sku_id = #{skuId}
          AND locked_stock &gt;= #{quantity}
    </update>

    <!--
        取消和超时关闭阶段把订单锁定库存释放回可售库存。
        该 SQL 与支付确认互斥依赖订单行锁保护，避免同一订单重复释放或边支付边取消。
    -->
    <update id="releaseLockedStock">
        UPDATE inventory
        SET locked_stock = locked_stock - #{quantity},
            available_stock = available_stock + #{quantity},
            updated_at = CURRENT_TIMESTAMP(6)
        WHERE sku_id = #{skuId}
          AND locked_stock &gt;= #{quantity}
    </update>

</mapper>
