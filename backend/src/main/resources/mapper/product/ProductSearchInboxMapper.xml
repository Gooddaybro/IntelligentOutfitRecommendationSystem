<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recommendation.intelligentoutfitrecommendationsystem.product.search.sync.ProductSearchInboxMapper">
    <select id="exists" resultType="boolean">
        SELECT CASE WHEN COUNT(*) &gt; 0 THEN TRUE ELSE FALSE END
        FROM product_search_inbox
        WHERE consumer_name = #{consumerName} AND event_id = #{eventId}
    </select>

    <insert id="insert">
        INSERT INTO product_search_inbox (consumer_name, event_id, spu_id, processed_at)
        VALUES (#{consumerName}, #{eventId}, #{spuId}, #{processedAt})
    </insert>
</mapper>
