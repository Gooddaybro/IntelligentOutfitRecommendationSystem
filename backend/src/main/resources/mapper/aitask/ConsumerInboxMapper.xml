<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recommendation.intelligentoutfitrecommendationsystem.aitask.mapper.ConsumerInboxMapper">
    <insert id="insertInbox">
        INSERT INTO consumer_inbox (consumer_name, event_id, task_id, processed_at)
        VALUES (#{consumerName}, #{eventId}, #{taskId}, #{processedAt})
    </insert>
    <select id="exists" resultType="boolean">
        SELECT CASE WHEN COUNT(*) &gt; 0 THEN TRUE ELSE FALSE END
        FROM consumer_inbox
        WHERE consumer_name = #{consumerName} AND event_id = #{eventId}
    </select>
</mapper>
