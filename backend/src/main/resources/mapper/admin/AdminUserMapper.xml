<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recommendation.intelligentoutfitrecommendationsystem.admin.mapper.AdminUserMapper">
    <sql id="adminUserColumns">
        u.id AS user_id, u.username, up.nickname, u.email, u.phone,
        CASE WHEN u.status = 'disabled' THEN 'DISABLED' ELSE 'ACTIVE' END AS status,
        u.created_at AS registered_at, COUNT(o.id) AS order_count,
        COALESCE(SUM(CASE WHEN o.status IN ('PAID', 'SHIPPED', 'COMPLETED')
                          THEN o.total_amount ELSE 0 END), 0) AS paid_amount
    </sql>

    <!--
        Admin user rows combine account profile data with paid-order aggregates for the console.
        The status CASE preserves the public API vocabulary while writes still use canonical database values.
    -->
    <select id="findUsers" resultType="com.recommendation.intelligentoutfitrecommendationsystem.admin.dto.AdminUserResponse">
        SELECT <include refid="adminUserColumns"/>
        FROM user_account u
        LEFT JOIN user_profile up ON up.user_id = u.id
        LEFT JOIN sales_order o ON o.user_id = u.id
        GROUP BY u.id, u.username, up.nickname, u.email, u.phone, u.status, u.created_at
        ORDER BY u.id ASC
    </select>

    <!--
        Reload after status changes uses the same projection as the list route so audit and response data
        reflect database-normalized account state rather than request payload assumptions.
    -->
    <select id="findUserById" resultType="com.recommendation.intelligentoutfitrecommendationsystem.admin.dto.AdminUserResponse">
        SELECT <include refid="adminUserColumns"/>
        FROM user_account u
        LEFT JOIN user_profile up ON up.user_id = u.id
        LEFT JOIN sales_order o ON o.user_id = u.id
        WHERE u.id = #{userId}
        GROUP BY u.id, u.username, up.nickname, u.email, u.phone, u.status, u.created_at
    </select>

    <update id="updateUserStatus">
        UPDATE user_account
        SET status = #{status}, updated_at = CURRENT_TIMESTAMP(6)
        WHERE id = #{userId}
    </update>
</mapper>
