<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recommendation.intelligentoutfitrecommendationsystem.payment.mapper.PaymentMapper">

    <insert id="insertPayment"
            parameterType="com.recommendation.intelligentoutfitrecommendationsystem.payment.model.Payment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payment (
            payment_no,
            order_id,
            order_no,
            user_id,
            amount,
            channel,
            status,
            transaction_id,
            provider_trade_no,
            provider_payload,
            paid_at
        ) VALUES (
            #{paymentNo},
            #{orderId},
            #{orderNo},
            #{userId},
            #{amount},
            #{channel},
            #{status},
            #{transactionId},
            #{providerTradeNo},
            #{providerPayload},
            #{paidAt}
        )
    </insert>

    <!--
        重复点击支付时只返回已经成功的支付流水，不再次转换库存。
        orderId 是内部关联边界，避免仅凭前端可见的 orderNo 做幂等判断。
    -->
    <select id="findSuccessByOrderId"
            resultType="com.recommendation.intelligentoutfitrecommendationsystem.payment.model.Payment">
        SELECT
            id,
            payment_no,
            order_id,
            order_no,
            user_id,
            amount,
            channel,
            status,
            transaction_id,
            provider_trade_no,
            provider_payload,
            paid_at,
            created_at,
            updated_at
        FROM payment
        WHERE order_id = #{orderId}
          AND status = 'SUCCESS'
    </select>

    <!--
        Real payment channels can leave an order in PENDING while the user refreshes or retries.
        Return the live attempt instead of creating a second PENDING row for the same order.
    -->
    <select id="findPendingByOrderId"
            resultType="com.recommendation.intelligentoutfitrecommendationsystem.payment.model.Payment">
        SELECT
            id,
            payment_no,
            order_id,
            order_no,
            user_id,
            amount,
            channel,
            status,
            transaction_id,
            provider_trade_no,
            provider_payload,
            paid_at,
            created_at,
            updated_at
        FROM payment
        WHERE order_id = #{orderId}
          AND status = 'PENDING'
    </select>

    <select id="findByPaymentNo"
            resultType="com.recommendation.intelligentoutfitrecommendationsystem.payment.model.Payment">
        SELECT
            id,
            payment_no,
            order_id,
            order_no,
            user_id,
            amount,
            channel,
            status,
            transaction_id,
            provider_trade_no,
            provider_payload,
            paid_at,
            created_at,
            updated_at
        FROM payment
        WHERE payment_no = #{paymentNo}
    </select>

    <!--
        Provider callbacks mutate payment and order state without user JWT, so they must lock the
        Java-created payment row before validating channel, amount and order consistency.
    -->
    <select id="findByPaymentNoForUpdate"
            resultType="com.recommendation.intelligentoutfitrecommendationsystem.payment.model.Payment">
        SELECT
            id,
            payment_no,
            order_id,
            order_no,
            user_id,
            amount,
            channel,
            status,
            transaction_id,
            provider_trade_no,
            provider_payload,
            paid_at,
            created_at,
            updated_at
        FROM payment
        WHERE payment_no = #{paymentNo}
        FOR UPDATE
    </select>

    <!--
        Payment success is updated by paymentNo and only from non-success states.
        This guards repeated provider callbacks from creating a second stock confirmation path.
    -->
    <update id="markPaymentSuccess">
        UPDATE payment
        SET status = 'SUCCESS',
            provider_trade_no = #{providerTradeNo},
            transaction_id = #{transactionId},
            paid_at = #{paidAt},
            updated_at = CURRENT_TIMESTAMP(6)
        WHERE payment_no = #{paymentNo}
          AND status != 'SUCCESS'
    </update>

    <insert id="insertCallbackLog"
            parameterType="com.recommendation.intelligentoutfitrecommendationsystem.payment.model.PaymentCallbackLog"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payment_callback_log (
            channel,
            payment_no,
            order_no,
            provider_trade_no,
            event_type,
            raw_body,
            headers,
            signature_valid,
            handled,
            failure_reason
        ) VALUES (
            #{channel},
            #{paymentNo},
            #{orderNo},
            #{providerTradeNo},
            #{eventType},
            #{rawBody},
            #{headers},
            #{signatureValid},
            #{handled},
            #{failureReason}
        )
    </insert>

</mapper>
